<form method="post" action="{{ path('app_personaje_disciplina_delete', {'id': personaje_disciplina.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personaje_disciplina.id) }}">
    <button class="btn">Delete</button>
</form>
